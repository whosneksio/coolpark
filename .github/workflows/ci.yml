name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest

    env:
      # Placeholder only: db.ts asserts DATABASE_URL at import, nothing connects during build.
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/coolpark
      # Placeholder only: redis connects lazily, nothing connects during build.
      REDIS_URL: redis://localhost:6379
      # Placeholder only: used to build email links at request time.
      APP_URL: http://localhost:3000

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: package.json

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Typegen
        run: bunx next typegen

      - name: Typecheck
        run: bun run typecheck

      - name: Test
        run: bun test

      - name: Build
        run: bun run build